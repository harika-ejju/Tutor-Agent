services:
  - type: web
    name: tutor-agent-backend
    runtime: python
    buildCommand: ./build.sh
    startCommand: python start_server.py
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
      - key: PORT
        sync: false